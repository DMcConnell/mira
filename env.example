# Mira Smart Mirror - Environment Configuration Example
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp env.example .env
# 2. Edit .env with your actual values
# 3. NEVER commit .env to git (it's in .gitignore)
#
# Docker Compose will automatically read .env from the project root

# ============================================
# Authentication & Security
# ============================================

# JWT Secret - Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# CRITICAL: Change this before production deployment!
JWT_SECRET=dev-secret-change-in-production

# PIN for authentication (4-6 digits recommended)
MIRA_PIN=1234

# Optional: Admin PIN with elevated privileges
# MIRA_ADMIN_PIN=9999

# ============================================
# Service Configuration
# ============================================

# Redis Configuration
REDIS_URL=redis://redis:6379

# Control Plane URL (internal docker network)
CONTROL_PLANE_URL=http://control-plane:8090

# Control Plane Database Path (shared volume)
CONTROL_PLANE_DB=/app/data/control_plane.db

# Backend Data Directory
DATA_DIR=/app/data

# ============================================
# Provider Modes
# ============================================

# Weather provider mode: mock or live
PROVIDERS_WEATHER_MODE=mock

# News provider mode: mock or live
PROVIDERS_NEWS_MODE=mock

# ============================================
# Optional: API Keys (for live providers)
# ============================================

# OpenWeatherMap API Key (if using live weather)
# OPENWEATHER_API_KEY=your_api_key_here

# News API Key (if using live news)
# NEWS_API_KEY=your_api_key_here

# Google Calendar API credentials (if using real calendar)
# GOOGLE_CALENDAR_CLIENT_ID=your_client_id
# GOOGLE_CALENDAR_CLIENT_SECRET=your_client_secret

# ============================================
# Development/Debugging
# ============================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Enable debug mode (verbose logging)
DEBUG=false

# ============================================
# Production Only
# ============================================

# HTTPS/SSL (when deploying with reverse proxy)
# SSL_CERT_PATH=/etc/ssl/certs/mira.crt
# SSL_KEY_PATH=/etc/ssl/private/mira.key

# Allowed CORS origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

